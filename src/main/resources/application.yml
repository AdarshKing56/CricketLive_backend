server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cricket-live-app?createDatabaseIfNotExist=true
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQL8Dialect
    properties:
      hibernate:
      formate_sql: true
    show-sql: true



